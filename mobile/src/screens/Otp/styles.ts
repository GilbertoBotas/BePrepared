import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      marginTop: 110,
      alignItems: "center",
      gap: 24,
    },
    descriptionText: {
      maxWidth: 300,
      textAlign: "center",
      fontSize: 14,
      fontFamily: "Inter_400Regular",
    }
  });